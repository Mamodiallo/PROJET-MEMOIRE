# etl/recode.py
import pandas as pd

def recode_df(df: pd.DataFrame) -> pd.DataFrame:
    """
    Exemple minimal : renvoie df tel quel.
    Ajoutez ici vos recodages (binning, mappage de catégories…).
    """
    return df
